# Homebrew
if [[ -f /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# Editor
export EDITOR='vim'
export VISUAL='vim'

# Browser
export BROWSER='open'

# PATH
export PATH="$HOME/.local/bin:$PATH"
